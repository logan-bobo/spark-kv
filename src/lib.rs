//! a simple implementation of a key value store that supports
//! key value setting, retrival and removal.

/// kv implementation exposing the [KvStore] type
pub mod kv;

/// wal implementation exposing the [wal] type
mod wal;
